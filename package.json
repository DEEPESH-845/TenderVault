{
  "name": "tendervault",
  "version": "1.0.0",
  "private": true,
  "description": "Tamper-proof, time-locked government tender bid management SaaS",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
